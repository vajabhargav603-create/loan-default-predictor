services:
  - type: web
    name: loanguard-ai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn week 4.backend.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
